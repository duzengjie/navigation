spring:
  thymeleaf:
    mode: HTML
    cache: false
    prefix: classpath:/templates
  servlet:
    multipart:
      max-file-size: 10MB
  datasource:
    url: jdbc:mysql://192.168.3.10:3306/navigation?serverTimezone=GMT&userSSL=false&characterEncoding=utf-8   #生产读写分离
    username: root
    password: duzengjie@0314
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      auto-commit: true
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      maximum-pool-size: 10
      minimum-idle: 10
      pool-name: MyHikariCP
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
